# Offline Sync

Sync runs on foreground, connectivity recovery, authentication restoration, and manual request. It revalidates the account before processing, executes dependencies first, refreshes affected queries, and never treats a lost response as proof of authoritative success.