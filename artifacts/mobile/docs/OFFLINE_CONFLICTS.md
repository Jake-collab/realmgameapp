# Offline Conflicts

Finalized and protected server domains win. Mutable creator drafts use base/local/server versions and field-aware three-way merge. If both sides changed the same field, the user sees an explicit conflict choice instead of silent overwrite.