# Offline Media

Local asset records preserve an app-private file reference, ownership, domain, hash metadata, retry count, and upload state. Missing files become user attention items. Cleanup retains unsynced evidence and only removes completed temporary assets under policy.