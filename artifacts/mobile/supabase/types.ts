/**
 * Canonical Supabase types for the mobile app.
 *
 * Keep one schema source until the connected project can generate a fresh
 * version with `supabase gen types typescript`.
 */
export { type Database, type Json, type Tables, type Enums } from '../lib/supabase/database.types';